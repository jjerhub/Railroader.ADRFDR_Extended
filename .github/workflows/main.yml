name: Do zip on release
on:
  release:
    types:
      - published
  workflow_dispatch:
      
jobs:
  do_zip:
    secrets: inherit
    uses: jjerhub/Utilities.GithubWorkflows/.github/workflows/MakeRailloaderModZip.yml@master
    with:
      mod-name: 'ADRFDR_Extended'
